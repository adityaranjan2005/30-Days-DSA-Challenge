Problem:

Write a program to find the area of a rectangle. If the input or output value is floating point roundoff to nearest integer.

Input Description:
First line consists of input number

Output Description:
Output consists of area of rectangle

Sample Input :
2 4
Sample Output :
8

Solution:

const input = prompt("Enter two space-separated floating-point numbers:");
const [a, b] = input.split(" ").map(parseFloat);

const c = Math.round(a);
const d = Math.round(b);

const area = Math.round(c * d);

console.log(area);
